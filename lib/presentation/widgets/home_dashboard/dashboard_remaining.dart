import 'package:flutter/material.dart';

class DashboardRemaining extends StatelessWidget {
  const DashboardRemaining({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
